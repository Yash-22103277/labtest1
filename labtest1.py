import numpy as np
import matplotlib.pyplot as plt

current = np.array([])

a = ([{'name', str}, {'quantity', int}])

item = np.array([{'yash', 4}, {'harsh', 10}, {'manas', 9}])

for i in current:
    ind = np.where(item['id' == i])
    print(ind)
    item[ind]['quantity']
    print(item[ind]['quantity'])

print(item)
print(a)